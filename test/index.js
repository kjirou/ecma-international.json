const assert = require('assert');
const tv4 = require('tv4');


describe('ecma-international.json', () => {

  it('should be', () => {
    const ecmaInternatinalJsonData = require('../ecma-international.json');
  });
});
